import React from 'react';
import Slider from '../Slider/Slider';
import EstateRoot from '../EstateRoot/EstateRoot';

const Home = () => {
    return (
        <div>
            <Slider />
            <EstateRoot />
        </div>
    );
};

export default Home;